<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>

  </head>
  <body>
    <h1> Lista de Quizzes </h1>
    	<ul>
		    <% for(var i=0; i<quizzes.length; i++) {%>
		      <li><%= quizzes[i].question %> : <%= quizzes[i].answer %></li>
		    <% } %>
		</ul>
	</body>
</html>